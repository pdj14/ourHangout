# Our Hangout Mobile Apps

The repository keeps the current and previous mobile applications separate:

- `app/`: current app used for development and release builds
- `app_old/`: previous app retained for comparison and migration reference

Run commands from the relevant application directory. The current app is the
default project:

```powershell
cd app
npm.cmd run typecheck
npm.cmd start
```
